export const selectCityName = (state) => state.slice.name;
export const selectMainWeather = (state) => state.slice.main;
export const selectIconWeather = (state) => state.slice.icon;
export const selectTemperatureWeather = (state) => state.slice.temp;
export const selectHumidityWeather = (state) => state.slice.humidity;
export const selectWindWeather = (state) => state.slice.wind;

